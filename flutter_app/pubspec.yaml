name: flutter_app
description: Flutter client for the smart home IoT plan.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^9.7.4
  record: ^5.0.4
  speech_to_text: ^6.6.0
  web_socket_channel: ^2.4.0
  permission_handler: ^11.0.0
  fl_chart: ^0.66.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
